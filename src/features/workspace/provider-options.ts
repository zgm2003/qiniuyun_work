import type { ProviderName } from "./workspace-context";

export const DEFAULT_PRODUCT_PROVIDER: Extract<ProviderName, "openai-compatible"> = "openai-compatible";
export const DEFAULT_PRODUCT_MODEL = "gpt-5.5";
